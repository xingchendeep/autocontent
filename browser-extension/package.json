{
  "name": "autocontent-pro-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit && node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.25.0",
    "typescript": "^5.4.0"
  }
}
